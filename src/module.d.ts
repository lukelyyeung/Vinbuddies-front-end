declare namespace reduxRice {
    interface AuthState {
        isAuthenticated: boolean;
        isSignedUp: boolean;
    }
    interface NavigationState {
        isOpen: boolean;
    }
    interface SignUpState {
    }
    
}